{
  "name": "@borg/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@borg/context": "workspace:*",
    "@borg/core": "workspace:*",
    "@borg/permissions": "workspace:*",
    "@borg/tools": "workspace:*"
  }
}
